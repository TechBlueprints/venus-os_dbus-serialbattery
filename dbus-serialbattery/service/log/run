#!/bin/sh
exec 2>&1
LOG_DIR="$(dirname "$(readlink -f "$0")")"
SVC_DIR="${LOG_DIR%/log}"
PORT_NAME="${SVC_DIR##*/dbus-serialbattery.}"
exec multilog t s1500000 n20 "/var/log/dbus-serialbattery.$PORT_NAME"
